# Extension version.
#
# All modern extensions should use 2 here. Version 1 uses a chassis.pp
# instead, which requires a deprecated feature of Puppet.
#
# Values: 2
version: 2

# default config overwrite created certs and keys
chassis_openssl:
    cert:
        replace: yes
    key:
        replace: yes
